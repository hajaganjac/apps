
  # High-fidelity mobile UI frames

  This is a code bundle for High-fidelity mobile UI frames. The original project is available at https://www.figma.com/design/cFtM1wYV9X95OyBMaLG1Jw/High-fidelity-mobile-UI-frames.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  